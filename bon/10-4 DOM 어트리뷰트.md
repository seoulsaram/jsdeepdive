
## 어트리뷰트 (=속성)

어트리뷰트는 HTML을 더욱 강력하게 만들어주는 요소다.
HTML요소들의 행동, 연결 기능 등을 정의하게 만들어준다.
HTML요소는 여러 개의 어트리뷰트를 가질 수 있다.

HTML문서가 파싱될 때 어트리뷰트는 어트리뷰트 노드로 변환되어
요소 노드와 연결된다.

![[스크린샷 2025-07-22 오후 5.01.12.png]]


### 어트리뷰트 객체가 만들어지는 과정
![[스크린샷 2025-08-27 오전 9.21.15.png]]
1. HTML 태그 안에 `id`, `class`, `src` 같은 속성을 하나씩 쓸 때마다, 브라우저는 각각을 속성 노드(attribute node) 로 따로 만들어 둔다.
2. 그리고 그 만들어진 속성 노드들을 전부 모아서 `attributes`라는 프로퍼티 안에 넣어둔다.
3. `attributes` 프로퍼티의 타입은 NamedNodeMap로, 유사 배열 객체이자 이터러블이다.

##### 💡 attributes프로퍼티 직접 확인해보기
![[스크린샷 2025-08-27 오전 8.54.54.png]]


### `Element.prototype.attributes`

- getter만 존재하는 접근자 프로퍼티

```js
<input id="user" type="text" value="bon"/>


// 요소 노드의 모든 어트리뷰트가 들어있는 NamedNodeMap 객체 반환
const { attributes } = document.getElementById('user');

console.log(attributes);
//NamedNodeMap {0: id, 1: type, 2: value, id: id, type: type, value: value, length: 3}
```

---

### 어트리뷰트 조작

#### `Element.prototype.getAttribute`

- 어트리뷰트 값 참조 시 사용하는 메서드
- `Element.prototype.getAttribute(attributeName)`

```js
<input id="user" type="text" value="bon"/>

const input = document.getElementById('user');
console.log(input.getAttribute('id')); //user
```


#### `Element.prototype.setAttribute`

- 어트리뷰트 값 변경 시 사용하는 메서드
- `Element.prototype.setAttribute(attributeName, attributeValue)`

```js
<input id="user" type="text" value="bon"/>

const input = document.getElementById('user');
input.setAttribute('value','가연'); //input에 들어있는 값이 변경됨
```



#### `Element.prototype.hasAttribute`

- 특정 어트리뷰트 존재 여부 확인하는 메서드
- `Element.prototype.hasAttribute(attributeName)`

#### `Element.prototype.removeAttribute`

- 어트리뷰트 삭제
- `Element.prototype.removeAttribute(attributeName)`

---

## HTML 어트리뷰트 vs. DOM 어트리뷰트

HTML요소에 넣은 어트리뷰트가 `attributes` 프로퍼티에 모아져있지만,
이에 대응되는 프로퍼티들은 `element`객체 바로 밑에도 생성되어 있다.
이것을 DOM 프로퍼티라고 한다.

```js
<input id="user" type="text" value="bon"/>


const input = document.getElementById('user');

console.log(input.attributes.id.nodeValue); // user
//DOM property
console.log(input.id); //user


console.log(input.attributes.type.nodeValue); // text
//DOM property
console.log(input.type); //text
```

DOM 프로퍼티는 setter, getter 모두 존재하는 접근자 프로퍼티이므로
참조와 변경이 모두 가능하다.


### 두 가지가 중복으로 존재하는 이유

중복으로 존재하는 것 같지만 그렇지 않음.

```js
<input id="user" type="text" value="bon"/>


const input = document.getElementById('user');

input.value = "changed!";
console.log(input.value);            // "changed!" (프로퍼티는 바뀜)
console.log(input.getAttribute("value")); // "bon" (HTML 원본은 그대로)

```

요소 노드는 2개의 상태를 가지고 있다.

1. 웹페이지를 처음 표시하거나 새로고침 할 경우를 위한 초기 상태
	- 요소 노드의 초기 상태는 어트리뷰트 노드가 관리
	- 이 값은 사용자 입력에 의해 상태가 변경되어도 변하지 않고 HTML 요소의 초기 상태를 그대로 유지한다.
	- 만약 이 초기 상태를 취득/변경하려면 `getAttribute / setAttribute`메서드를 사용한다
2. 초기 상태에서 변화한 최신 상태
	- 최신 상태는 DOM 프로퍼티가 관리


---

## 요소에 적용되어 있는 CSS스타일 참조

element.style은 인라인 스타일만 반환함
HTML요소에 적용되어 있는 
모든 CSS 스타일을 참조하려면 `getComputedStyle` 메서드를 사용한다.

### `window.getComputedStyle(element[, pseudo])`

- 첫번째 인수(element)로 전달한 요소 노드에 적용되어 있는 평가된 스타일을 CSSStyleDeclaration객체에 담아 반환
- 평가된 스타일이란 모든 스타일(Agent style, 상속된 스타일 등)이 조합되어 최종적으로 적용된 스타일을 말함

```js
<input id="user" type="text" value="bon"/>

<style>
  input{
    background:blue;
  }
</style>



const input = document.getElementById('user');

console.log(input.style.background); //
console.log(getComputedStyle(input)['background-color']); //rgb(0, 0, 255)
```

의사 요소 스타일 취득 :
```js
<div class="box"/>

<style>
 .box:before{content: 'Hello';}
</style>

const box = document.querySelector('.box');

// 의사 요소 :before의 스타일 취득
const computedStyle = window.getComputedStyle(box, ':before');
console.log(computedStyle.content); // "Hello"
```


---

돔 표준 문서

DOM Level 3
https://www.w3.org/TR/DOM-Level-3-Core/def-index.html

DOM Level 4
https://ko.htmlspecs.com/dom/