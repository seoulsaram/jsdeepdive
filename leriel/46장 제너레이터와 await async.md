### 1. 제너레이터와 async/await의 탄생 배경

JavaScript는 단일 스레드로 동작합니다. 이로 인해 시간이 오래 걸리는 비동기 작업(네트워크 요청, 파일 읽기 등)을 효율적으로 처리하기 위해 콜백, 프라미스 같은 다양한 방법이 등장했죠. 하지만 복잡한 비동기 코드는 가독성이 떨어지는 '콜백 지옥'이나 긴 `.then()` 체인을 만들곤 했습니다.

**제너레이터**와 **async/await**는 이러한 문제를 해결하고, 비동기 코드를 마치 동기 코드처럼 **순차적이고 읽기 쉽게** 작성하기 위해 등장한 강력한 도구입니다.

---

### 2. 제너레이터 (Generator)

### ✅ 46.1 제너레이터란?

**제너레이터**는 함수의 실행을 **일시 중지**하고 **재개**할 수 있는 특별한 함수입니다. 함수 전체를 한 번에 실행하는 일반 함수와 달리, 필요할 때마다 조금씩 실행하며 값을 '생산'해냅니다.

- **키워드:** `function*`
- **일시 중지:** `yield`
- **비유:** 동영상 플레이어의 ⏯️ 버튼과 같습니다. `yield`는 일시 중지, `next()`는 재생 버튼이죠.

### ✅ 46.2 제너레이터 함수의 정의 & 46.4 일시 중지와 재개 (예제)

제너레이터 함수는 `function*` 키워드로 정의하며, `yield` 키워드를 통해 값을 반환하며 실행을 멈춥니다.

```jsx
function* myGenerator() {
  console.log('1. 첫 번째 yield 전');
  yield 1; // 🛑 여기서 일시 중지
  console.log('2. 두 번째 yield 전');
  yield 2; // 🛑 여기서 일시 중지
  console.log('3. 세 번째 yield 전');
  yield 3; // 🛑 여기서 일시 중지
  console.log('4. 함수 끝');
  return '완료'; // 반환 값
}

// 💡 실행 방법
const gen = myGenerator(); // 제너레이터 함수를 호출해도 바로 실행되지 않습니다.

console.log(gen.next()); // { value: 1, done: false }  - '1. 첫 번째 yield 전' 출력
console.log(gen.next()); // { value: 2, done: false }  - '2. 두 번째 yield 전' 출력
console.log(gen.next()); // { value: 3, done: false }  - '3. 세 번째 yield 전' 출력
console.log(gen.next()); // { value: '완료', done: true } - '4. 함수 끝' 출력
console.log(gen.next()); // { value: undefined, done: true }
```

- `next()` 메서드를 호출할 때마다 `yield` 지점까지 코드가 실행됩니다.
- `next()`는 `{ value: ..., done: ... }` 형태의 객체를 반환합니다. `done: true`는 함수 실행이 완료되었음을 의미합니다.

### ✅ 46.5 제너레이터의 활용

### 46.5.1 이터러블의 구현 (예제)

제너레이터는 **이터레이터 프로토콜**을 자동으로 구현해 주기 때문에, 커스텀 이터러블을 아주 쉽게 만들 수 있습니다.

```jsx
// 무한한 피보나치 수열을 만드는 제너레이터
function* fibonacci() {
  let a = 0, b = 1;
  while (true) {
    yield a;
    [a, b] = [b, a + b];
  }
}

const fib = fibonacci();
console.log(fib.next().value); // 0
console.log(fib.next().value); // 1
console.log(fib.next().value); // 1
console.log(fib.next().value); // 2
console.log(fib.next().value); // 3
// ... 메모리 부담 없이 무한한 시퀀스를 생성
```

---

### 46.5.2 비동기 처리 (이론 & 예제)

제너레이터는 비동기 작업을 `yield`로 잠시 멈추고, 작업이 완료된 후 `next()`로 재개하는 방식으로 비동기 코드를 동기 코드처럼 작성할 수 있습니다. 이것이 **async/await의 기본 원리**입니다.

```jsx
// 프라미스 기반의 간단한 비동기 함수

// 제너레이터를 이용한 비동기 처리
function* runAsync() {
  console.log('⏳ 시작');
  const res = api()
  yield res
  const res2 = api2()
  yield res2
  
  return 
  console.log('✅ 또 1초 대기 후 재개');
}
{ value : ~~~, done : true/ false }
// 💡 제너레이터 실행기(Executor)
// async/await가 내부적으로 이와 비슷하게 동작합니다.
function executor(generator) {
  const iterator = generator();
  function nextStep(result) {
    if (result.done) return;
    result.value.then(() => nextStep(iterator.next()));
  }
  nextStep(iterator.next());
}

executor(runAsync);

const example = runAsync()

example.next('example')
```

---

### 3. async/await

### ✅ 46.6.1 async 함수 & 46.6.2 await 키워드 (예제)

**async/await**는 **프라미스 기반의 비동기 코드를 동기 코드처럼** 작성하게 해주는 ES8의 문법적 설탕(Syntactic Sugar)입니다.

- `async function`: 함수 앞에 `async`를 붙이면, 이 함수는 항상 프라미스를 반환합니다. 이 함수 안에서만 `await`를 사용할 수 있습니다.
- `await`: **프라미스**가 해결(resolve)될 때까지 `async` 함수 내부의 실행을 **일시 중지**시킵니다.

```jsx
// async/await를 이용한 비동기 처리
const delay = (ms) => new Promise(resolve => setTimeout(resolve, ms));

async function processData() {
  console.log('1️⃣ 데이터 처리 시작');

  await delay(2000); // 2초 대기
  console.log('2️⃣ 2초 대기 후 다음 단계');

  const response = await fetch('https://jsonplaceholder.typicode.com/posts/1'); // 네트워크 요청 대기
  const data = await response.json(); // JSON 변환 대기

  console.log('3️⃣ 받아온 데이터:', data.title);
  console.log('4️⃣ 데이터 처리 완료!');
}

processData();
```

- 위 코드의 실행 흐름은 매우 직관적입니다. 마치 `delay`와 `fetch`가 동기적으로 실행되는 것처럼 보이죠.

---

### ✅ 46.6.3 에러 처리 (예제)

`async/await`의 에러 처리는 일반적인 동기 코드처럼 **`try...catch`** 블록을 사용합니다. `await`로 기다리는 프라미스가 `reject`되면 `catch` 블록이 실행됩니다.

```jsx
async function fetchDataWithError() {
  try {
    const response = await fetch('https://invalid-url.com/data');
    if (!response.ok) {
      throw new Error(`네트워크 응답 오류: ${response.status}`);
    }
    const data = await response.json();
    console.log('데이터:', data);
  } catch (error) {
    console.error('😱 데이터 가져오기 실패:', error.message);
  }
}

fetchDataWithError();
```

---